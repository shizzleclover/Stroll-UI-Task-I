import 'package:flutter/material.dart';

class CustomBappbar extends StatelessWidget {
  const CustomBappbar({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      height: 80,
      padding: const EdgeInsets.symmetric(
        vertical: 5,
        horizontal: 10,
      ),
    //!Add color after running themes
    );
  }
}